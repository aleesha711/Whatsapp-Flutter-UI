import 'package:flutter/material.dart';

class CallHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context){
    return new Container(
      child: new Center(
        child: new Text('Call history here...', style: new TextStyle(fontSize: 20.0),),
      )
    );
  }
}